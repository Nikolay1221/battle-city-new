# 🚀 Battle City AI: Google Colab Setup

Чтобы запустить этот проект в Google Colab или на чистом Linux сервере, недостаточно просто `pip install`.
Эмулятор `nes-py` требует системные библиотеки для графики (SDL2), даже если мы не выводим картинку на экран.

## Шаг 1. Установка системных библиотек (System Deps)
Выполните эту ячейку **первой** (перед pip install).

```bash
!apt-get update
!apt-get install -y libsdl2-dev libsdl2-image-dev libsdl2-mixer-dev libsdl2-ttf-dev freetype2-demos
```

## Шаг 2. Установка Python библиотек
После того как `sdl2` установлен, можно ставить пакеты.

```bash
!pip install -r requirements.txt
```

*(Или вручную: `!pip install gymnasium stable-baselines3[extra] nes-py opencv-python shimmy`)*

## Шаг 3. Загрузка ROM
Не забудьте загрузить файл `BattleCity_fixed.nes` в папку Colab (значок папки слева -> перетащить файл), иначе эмулятор не найдет игру.

## Шаг 4. Запуск с GPU
В Colab выберите: **Runtime -> Change runtime type -> T4 GPU**.
Наш код (`train.py`) автоматически увидит CUDA и будет работать в 50 раз быстрее.
```python
!python train.py
```
